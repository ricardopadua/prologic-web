/**
 * @displayName Layout Index
 */

interface LayoutProps {
  title: string;
  subTitle: string;
}

const Layout = ({ title, subTitle }: LayoutProps) => (
      <>
      <div></div>
      </>
);

export default Layout;
